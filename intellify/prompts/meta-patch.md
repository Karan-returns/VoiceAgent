# Meta-Prompt — Post-Call Prompt Patcher (Loop 2)

<!--
The meta-prompt used by Loop 2. Will receive the extracted failure patterns from
the analysis JSON and produce a targeted diff/rewrite to ONE section of the base
system prompt only (not a full rewrite), so the next call starts smarter.
-->
