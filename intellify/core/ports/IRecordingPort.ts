// Recording port — call audio capture contract (implemented by LiveKit/mongo infra).
// Implements: start/stop recording and persist the mixed agent+customer audio
// as a wav/mp3 file on the server, returning the saved file path.
