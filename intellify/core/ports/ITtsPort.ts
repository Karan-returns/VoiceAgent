// TTS port — text-to-speech contract (implemented by ElevenLabs adapter).
// Implements: synthesize(text) -> streamed audio chunks, with the ability to
// stop synthesis instantly when the user interrupts.
