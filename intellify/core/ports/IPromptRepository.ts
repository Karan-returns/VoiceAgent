// Prompt repository port — prompt versioning contract (implemented by Mongo adapter).
// Implements: getActivePrompt(), saveVersion(patch, triggeredByCallId),
// listVersions() — backs Loop 2 evolution and rollback.
