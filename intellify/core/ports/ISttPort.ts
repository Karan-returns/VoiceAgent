// STT port — speech-to-text contract (implemented by Deepgram adapter).
// Implements: streaming transcribe API that emits interim + final transcripts
// with timestamps; core depends on this, never on the Deepgram SDK directly.
