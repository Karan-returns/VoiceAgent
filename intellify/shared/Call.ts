// Call metadata domain type — the top-level record stored per call.
// Implements: callId, status lifecycle, duration, timestamps, recording path,
// and the promptVersionId that was active during the call (audit trail link).
