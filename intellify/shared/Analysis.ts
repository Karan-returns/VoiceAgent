// Analysis domain types — the structured QA scorecard shape.
// Implements: AnalysisScorecard (rubric_score, sentiment_arc, call_flow, flags,
// agent_signals, improvement_areas), CallFlowStage enum, SentimentLabel enum,
// and the per-turn sentiment / call-flow item types.
