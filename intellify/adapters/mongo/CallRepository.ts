// CallRepository — implements ICallRepository over MongoDB.
// Implements: persist/load calls, transcript turns, recording metadata, and the
// analysis scorecard; queries for the dashboard (single + multi-call trends).
