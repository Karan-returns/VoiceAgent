// LiveKitTransport — implements ITransportPort over LiveKit WebRTC.
// Implements: room connect, subscribe to caller mic audio, publish agent TTS
// audio track, and barge-in flush. Translates LiveKit audio frames <-> core types.
