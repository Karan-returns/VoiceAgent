// Loop 2 patch tests — verify failure list -> targeted single-section patch.
// Will assert patch shape, that only one section changes, and a new PromptVersion
// is saved linked to the triggering callId.
